<%= form_for @cart do |f| %>
	<%= render 'shared/errors', object: @cart %>
	<%= f.label			:name %>
	<%= f.text_field	:name %>
	<%= f.label			:description %>
	<%= f.text_area		:description %>
	<%= f.submit class: "button" %>
<% end %>